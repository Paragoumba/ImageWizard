package fr.paragoumba.imagewizard;

import java.awt.*;
import java.awt.image.BufferedImage;

/**
 * Created by Paragoumba on 23/05/2017.
 */

public class Shapes {

    public BufferedImage rectangle(int width, int height){

        BufferedImage rectangle = new BufferedImage(width, height, BufferedImage.TYPE_INT_ARGB);
        //Graphics2D g2d = rectangle.getGraphics()

        for (int x = 0 ; x < width ; ++x){
            for (int y = 0 ; y < height ; ++y){

            }
        }

        return rectangle;
    }
}
